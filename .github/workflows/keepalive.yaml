name: Keep Token Monitor Alive

on:
  schedule:
    - cron: "*/10 * * * *"   # every 5 minutes
  workflow_dispatch:         # allow manual trigger

jobs:
  ping-site:
    runs-on: ubuntu-latest
    steps:
      - name: Install Puppeteer
        run: npm install puppeteer

      - name: Visit Token Monitor Page
        run: |
          node -e "
          const puppeteer = require('puppeteer');
          (async () => {
            const browser = await puppeteer.launch({ headless: true, args: ['--no-sandbox'] });
            const page = await browser.newPage();
            await page.goto('https://ismarty.vercel.com', { waitUntil: 'networkidle2', timeout: 60000 });
            console.log('✅ Token Monitor visited at', new Date().toISOString());
            await browser.close();
          })();
          "
